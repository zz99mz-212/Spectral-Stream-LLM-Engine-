---
phase: 01-metrics-trust-loop
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - spectralstream/compression/honest_metrics.py
  - spectralstream/compression/cli.py
  - tests/test_honest_metrics.py
autonomous: true
requirements:
  - METRICS-01
  - METRICS-04
user_setup: []
must_haves:
  truths:
    - A method whose rel_mse exceeds the gate threshold emits NO numeric compression ratio; its honest_metrics dict carries gated:True and ratio_vs_bf16:None (not a zero/1.0x).
    - A good method (rel_mse <= 0.05) emits both ratio_vs_bf16 (headline) and ratio_vs_fp32, derived byte-exactly from serialized_nbytes via dual_ratio.
    - At exactly rel_mse == 0.05 the gate is NOT triggered (strict > boundary, consistent with Phase-3 cascade acceptance).
    - serialized_nbytes returns the true recursive byte count for every payload shape (None/bytes/bytearray/ndarray/np.generic/dict/list/tuple/bool/int/float/str).
  artifacts:
    - spectralstream/compression/honest_metrics.py::ERROR_GATE_THRESHOLD (constant = 0.05)
    - spectralstream/compression/honest_metrics.py::apply_gate (pure chokepoint fn)
    - tests/test_honest_metrics.py (new, passing)
    - cli.py World-Model honest-metrics block routes through apply_gate
  key_links:
    - apply_gate is the ONLY ratio-emission decision point; both CLI blocks and future reporters must call it (not per-call-site thresholds).
    - The gate never drops the error metrics (rel_mse/cosine_sim/max_abs/snr_db) — only suppresses the ratio claim.
---

<objective>
Stand up the honest metrics-trust loop end-to-end as the walking skeleton: a central
`apply_gate()` chokepoint in `honest_metrics.py` that couples every compression ratio to its
reconstruction error, plus the test file that anchors it. A high-error method is gated
(marked, ratio suppressed); a good method keeps its byte-exact ratio.

Purpose: This is the highest-leverage honesty fix (closes BUG-02). Every later phase routes
through this gate, so it must be proven by a failing-then-passing test before any wiring exists.
Output: `honest_metrics.apply_gate`, `ERROR_GATE_THRESHOLD`, `tests/test_honest_metrics.py`,
and the World-Model CLI block gated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-metrics-trust-loop/01-RESEARCH.md
@.planning/phases/01-metrics-trust-loop/01-PATTERNS.md
@spectralstream/compression/honest_metrics.py
@spectralstream/compression/cli.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Create failing honest-metrics test file</name>
  <files>tests/test_honest_metrics.py, spectralstream/compression/honest_metrics.py</files>
  <read_first>
    - tests/test_certificate_comprehensive.py (header + sys.path.insert pattern lines 1-27; box-drawing banner line 30; industry_comparison contract lines 1263-1362)
    - spectralstream/compression/honest_metrics.py (full — existing serialized_nbytes lines 25-67, dual_ratio 133-147, end_to_end_error 83-130, ErrorMetrics 76-80)
  </read_first>
  <action>
    Write `tests/test_honest_metrics.py` (does NOT exist yet) mirroring the test_certificate
    conventions: first line `from __future__ import annotations`, then `import os`, `import sys`,
    `import numpy as np`, `import pytest`, `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))`.
    Import `serialized_nbytes, dual_ratio, end_to_end_error, apply_gate, ERROR_GATE_THRESHOLD`
    from `spectralstream.compression.honest_metrics`. Use box-drawing ASCII section banners.
    Include tests (these FAIL before GREEN because `apply_gate`/`ERROR_GATE_THRESHOLD` are undefined):
    - `test_good_method_emits_ratio`: orig=randn(8,8) float32, recon=orig+1e-4*noise; assert
      `end_to_end_error(...).rel_mse <= ERROR_GATE_THRESHOLD`; `res=apply_gate(orig.tobytes(), orig.size, err.rel_mse)`;
      `res["gated"] is False`, `res["ratio_vs_bf16"] is not None`, `res["ratio_vs_fp32"] is not None`.
    - `test_bad_method_gated`: recon=uncorrelated randn(8,8); `err.rel_mse > ERROR_GATE_THRESHOLD`;
      `res["gated"] is True`, `res["ratio_vs_bf16"] is None`, `res["ratio_vs_fp32"] is None`,
      `"rel_mse" in res["gate_reason"]`.
    - `test_boundary_exactly_threshold_not_gated`: `apply_gate(b"\x00"*64, 32, ERROR_GATE_THRESHOLD)["gated"] is False`.
    - `test_boundary_just_above_gated`: `apply_gate(b"\x00"*64, 32, ERROR_GATE_THRESHOLD+1e-6)["gated"] is True`.
    - `test_serialized_nbytes_shapes` parametrized over (None,0),(b"abc",3),(bytearray(b"xy"),2),
      (np.zeros((4,4),float32),64),(np.float32(1.0),4),({"a":b"xy","b":3},len("a")+2+8),
      ([b"ab",(4,4)],2+8+8),(True,1),(7,8),(2.5,8),("hi",2) — assert `serialized_nbytes(p)==e`.
    Do NOT implement `apply_gate` or `ERROR_GATE_THRESHOLD` — that is Task 2 (GREEN).
  </action>
  <verify>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "gate or boundary or serialized" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_honest_metrics.py` exists and begins with `from __future__ import annotations`.
    - Running the command above FAILS with `ImportError: cannot import name 'apply_gate'` (RED state proven) before Task 2.
    - The file contains `def test_bad_method_gated` and `def test_serialized_nbytes_shapes`.
  </acceptance_criteria>
  <done>Test file exists and fails to import `apply_gate`/`ERROR_GATE_THRESHOLD` (RED established).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement apply_gate + gate the World-Model CLI block</name>
  <files>spectralstream/compression/honest_metrics.py, spectralstream/compression/cli.py, tests/test_honest_metrics.py</files>
  <read_first>
    - spectralstream/compression/honest_metrics.py (constants lines 21-22; dual_ratio lines 133-147; ErrorMetrics 76-80)
    - spectralstream/compression/cli.py (World-Model honest-metrics block lines 838-867)
    - tests/test_honest_metrics.py (the file created in Task 1)
  </read_first>
  <action>
    In `honest_metrics.py` add after line 22 (`FP32_BYTES_PER_ELEMENT = 4`):
      `ERROR_GATE_THRESHOLD = 0.05  # rel_mse; strict > gate; consistent with Phase-3 cascade acceptance`
    Add after `dual_ratio` (after line 147) a pure function:
      `def apply_gate(payload, original_elements, rel_mse, threshold=ERROR_GATE_THRESHOLD) -> Dict[str, Any]:`
      Body: `ratios = dual_ratio(original_elements, payload)`; `gated = bool(rel_mse > threshold)`
      (STRICT `>`, never `>=`); return dict with keys `ratio_vs_bf16`
      (`ratios["ratio_vs_bf16"] if not gated else None`), `ratio_vs_fp32`
      (`ratios["ratio_vs_fp32"] if not gated else None`), `rel_mse` (float), `gated`, and
      `gate_reason` (`f"rel_mse {rel_mse:.4f} > {threshold}"` when gated else `""`).
    In `cli.py` World-Model block (lines 838-867): the import already brings `dual_ratio, end_to_end_error`
    — add `apply_gate` to that import. Replace the two ratio assignments (lines 846-848:
      `ratios = dual_ratio(original.size, data)` / `honest_metrics_dict["ratio_vs_fp32"]=...` /
      `honest_metrics_dict["ratio_vs_bf16"]=...`) so the ratio is produced by the gate. Keep computing
      `err = end_to_end_error(original, recon)` (lines 861-865) and the cosine_sim/max_abs/snr_db
      assignments unchanged; then call `honest_metrics_dict.update(apply_gate(data, original.size, err.rel_mse))`
      AFTER `err` is computed. Result: gated tensors get `gated:True`, `ratio_vs_bf16:None`; good
      tensors keep both ratios. Do NOT touch the summary print block (lines 1414-1417) — that is Plan 02.
  </action>
  <verify>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "gate or boundary or serialized" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def apply_gate" spectralstream/compression/honest_metrics.py` returns the new function.
    - `grep -n "ERROR_GATE_THRESHOLD = 0.05" spectralstream/compression/honest_metrics.py` matches.
    - `grep -n "apply_gate(" spectralstream/compression/cli.py` returns >= 1 match in the World-Model block.
    - `apply_gate(b"\x00"*64, 32, 0.0500)["gated"] is False`; `apply_gate(b"\x00"*64, 32, 0.0501)["gated"] is True`.
    - The pytest command exits 0 (all 4 gate/boundary + serialized_nbytes shape tests pass).
  </acceptance_criteria>
  <done>apply_gate implemented and the World-Model block routes through it; tests pass (GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compression output → reported ratio | `apply_gate` is the chokepoint where reconstruction error gates ratio emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing/Integrity | honest_metrics.apply_gate + cli.py | high | mitigate | Central chokepoint couples every ratio to `end_to_end_error`; gate suppresses ratio when `rel_mse > 0.05`; tests assert no ratio emitted for high-error methods. |
| T-01-02 | Tampering/Integrity | cli.py summary rendering | medium | mitigate | Gate emits explicit `gated:True` + `gate_reason` so a gated tensor is never rendered as a fabricated 0.0x/1.0x; CLI will render "GATED" (Plan 02). Block on medium. |
</threat_model>

<verification>
- `python -m pytest tests/test_honest_metrics.py -v -k "gate or boundary or serialized"` exits 0.
- Manual sanity (optional): a deliberately-bad (noisy) method → `honest_metrics_dict["gated"] is True` and `ratio_vs_bf16 is None`.
</verification>

<success_criteria>
- `apply_gate` exists in `honest_metrics.py`; threshold is the single `ERROR_GATE_THRESHOLD = 0.05` constant.
- High-error methods are gated (no numeric ratio) and good methods keep byte-exact ratios.
- `test_honest_metrics.py` exists and passes; covers gate coupling, strict-`>` boundary, and all `serialized_nbytes` payload shapes.
</success_criteria>

<artifacts>
Artifacts this phase produces (new symbols created by Plan 01):
- `spectralstream/compression/honest_metrics.py` :: `ERROR_GATE_THRESHOLD` (module constant, value `0.05`)
- `spectralstream/compression/honest_metrics.py` :: `apply_gate(payload, original_elements, rel_mse, threshold=ERROR_GATE_THRESHOLD) -> Dict[str, Any]`
- `tests/test_honest_metrics.py` (NEW FILE) :: `test_good_method_emits_ratio`, `test_bad_method_gated`, `test_boundary_exactly_threshold_not_gated`, `test_boundary_just_above_gated`, `test_serialized_nbytes_shapes`
- `cli.py` :: updated import (adds `apply_gate`); World-Model honest-metrics block (L838-867) now calls `apply_gate`
</artifacts>

<output>
Create `.planning/phases/01-metrics-trust-loop/01-01-SUMMARY.md` when done
</output>
