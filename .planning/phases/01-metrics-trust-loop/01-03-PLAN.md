---
phase: 01-metrics-trust-loop
plan: 03
type: tdd
wave: 3
depends_on:
  - 01-01
files_modified:
  - spectralstream/compression/literature_estimates.py
  - spectralstream/compression/certificate.py
  - tests/test_honest_metrics.py
  - benchmark_industry_comparison.json
autonomous: true
requirements:
  - METRICS-03
user_setup: []
must_haves:
  truths:
    - certificate.py contains zero hardcoded competitor constants (GPTQ/AWQ/SqueezeLLM/GGML Q literals gone) — only imports from literature_estimates.
    - Any external competitor reference is labeled "literature estimates, not measured here" in the industry_comparison dict AND rendered in to_text/to_markdown.
    - The orphaned benchmark_industry_comparison.json is deleted from the repo.
    - The existing industry_comparison dict contract is preserved (keys comparisons/beats_standard_quant/beats_int4/rank/better_than_count/total_compared; >=9 comparisons; current method included; booleans unchanged) so test_certificate* stay green.
  artifacts:
    - spectralstream/compression/literature_estimates.py (NEW: LITERATURE_ESTIMATES, LITERATURE_DISCLAIMER)
    - spectralstream/compression/certificate.py::_compute_industry_comparison imports + uses LITERATURE_ESTIMATES; adds disclaimer key; renders disclaimer in to_text/to_markdown
    - tests for METRICS-03 (no competitor literals + disclaimer + 9 entries + contract preserved)
  key_links:
    - The current-run tuple ("SpectralStream (current)", ...) stays computed from `ratio` in certificate.py, NOT moved to literature_estimates (Pitfall 3).
    - Deleting the JSON is a repo-file deletion (safe: zero references confirmed).
---

<objective>
Remove fabricated competitor comparisons: extract the 9 hardcoded competitor constants out of
`certificate.py` into a new `literature_estimates.py` module labeled "literature estimates, not
measured here", surface the disclaimer in the rendered certificate, preserve the existing
`industry_comparison` contract, and delete the orphaned `benchmark_industry_comparison.json`.

Purpose: METRICS-03 (fixes SEC-03). Removes the fabrication surface while keeping `test_certificate*`
green. Test-first: write the static + contract tests before refactoring certificate.py.
Output: literature_estimates module, certificate pure consumer, deleted JSON, passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-metrics-trust-loop/01-RESEARCH.md
@.planning/phases/01-metrics-trust-loop/01-PATTERNS.md
@spectralstream/compression/certificate.py
@tests/test_certificate.py
@tests/test_certificate_comprehensive.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Create literature_estimates module + METRICS-03 tests</name>
  <files>spectralstream/compression/literature_estimates.py, tests/test_honest_metrics.py, spectralstream/compression/certificate.py</files>
  <read_first>
    - tests/test_certificate_comprehensive.py (industry_comparison contract lines 1263-1362)
    - spectralstream/compression/certificate.py (_compute_industry_comparison lines 429-463)
    - benchmark_industry_comparison.json (confirm present at repo root)
  </read_first>
  <action>
    Create `spectralstream/compression/literature_estimates.py` (NEW) — first line
      `from __future__ import annotations`, then a module docstring declaring
      "LITERATURE ESTIMATES, NOT MEASURED HERE", then
      `from typing import List, Tuple`,
      `LITERATURE_DISCLAIMER = "literature estimates, not measured here"`,
      `LITERATURE_ESTIMATES: List[Tuple[str, float, str, str]]` = the 9 verbatim tuples from
      certificate.py lines 432-442: ("FP16 (baseline)",2.0,"2x storage savings","lossless"),
      ("INT8 quantization",4.0,"Standard 8-bit quantization","lossy"),
      ("INT4 quantization",8.0,"Standard 4-bit quantization","lossy"),
      ("NF4 (QLoRA)",4.0,"Normal float 4, QLoRA standard","lossy"),
      ("GPTQ 4-bit",8.0,"Post-training quantization","lossy"),
      ("AWQ 4-bit",8.0,"Activation-aware quantization","lossy"),
      ("GGML Q4_0",4.5,"llama.cpp Q4_0 quantization","lossy"),
      ("GGML Q8_0",2.5,"llama.cpp Q8_0 quantization","lossy"),
      ("SqueezeLLM",8.0,"Non-uniform quantization","lossy").
      Do NOT include the "SpectralStream (current)" row (computed from ratio at runtime).
    Append to `tests/test_honest_metrics.py` (Plan 01/02 file) a METRICS-03 section with:
    - `test_literature_disclaimer_present`: `"literature estimates" in LITERATURE_DISCLAIMER.lower()`
      and `len(LITERATURE_ESTIMATES) == 9`.
    - `test_no_competitor_literals_in_certificate_source`: read certificate.py source via pathlib and
      assert "GPTQ" not in src, "AWQ" not in src, "SqueezeLLM" not in src, "GGML Q" not in src
      (this will FAIL until Task 2 removes the literals).
    - `test_industry_comparison_contract_preserved`: build/load a CompressionCertificate-style
      `industry_comparison` via `LITERATURE_ESTIMATES` (replicate the certificate math minimally):
      assert keys present: comparisons, beats_standard_quant, beats_int4, rank, better_than_count,
      total_compared, disclaimer; `len(comparisons) >= 9`; the current-run row is computed from `ratio`
      not from LITERATURE_ESTIMATES; beats_standard_quant == (ratio > 4.0).
  </action>
  <verify>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "literature or competitor or contract" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `spectralstream/compression/literature_estimates.py` exists, starts with `from __future__ import annotations`, exports `LITERATURE_ESTIMATES` and `LITERATURE_DISCLAIMER`.
    - `grep -n "GPTQ 4-bit" spectralstream/compression/literature_estimates.py` returns the tuple.
    - `test_no_competitor_literals_in_certificate_source` FAILS (RED) because certificate.py still has literals.
    - The other two METRICS-03 tests are present.
  </acceptance_criteria>
  <done>literature_estimates module exists; METRICS-03 tests written with the competitor-literal guard in RED.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Refactor certificate.py + delete orphaned JSON</name>
  <files>spectralstream/compression/certificate.py, spectralstream/compression/literature_estimates.py, benchmark_industry_comparison.json, tests/test_honest_metrics.py</files>
  <read_first>
    - spectralstream/compression/certificate.py (imports block line ~30; _compute_industry_comparison lines 429-463; to_markdown lines 762-781; to_text lines 784-822)
    - spectralstream/compression/literature_estimates.py (created Task 1)
    - benchmark_industry_comparison.json (repo root — confirm orphaned)
  </read_first>
  <action>
    In `certificate.py`: add to the existing `from spectralstream.compression...` import group:
      `from spectralstream.compression.literature_estimates import (LITERATURE_ESTIMATES, LITERATURE_DISCLAIMER)`.
    In `_compute_industry_comparison` (lines 429-463): DELETE the 9 hardcoded tuples (lines 433-441) and
      build `comparisons = list(LITERATURE_ESTIMATES) + [("SpectralStream (current)", round(ratio, 1), "This run", "hybrid")]`.
      The `better_count`/`total_known`/`rank` math (lines 444-446) stays UNCHANGED. Add a `"disclaimer": LITERATURE_DISCLAIMER`
      key to the `self.industry_comparison` dict (after line 462). Keep every other existing key.
    In `to_markdown` (after line 780, before `return`): append
      `lines.append("")` then `lines.append(f"⚠️ {self.industry_comparison.get('disclaimer', '')}")`.
    In `to_text` (after the Industry Comparison section, before `return` at line 822): append a line
      `f"  ⚠️ {self.industry_comparison.get('disclaimer', '')}"`.
    Delete the orphaned `benchmark_industry_comparison.json` from the repo root (`git rm` if tracked, else `os.remove`); it has zero `.py`/`.json` references (confirmed via grep).
  </action>
  <verify>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "literature or competitor or contract" 2>&1 | tail -15</automated>
    <automated>python -m pytest tests/test_certificate.py tests/test_certificate_comprehensive.py -v 2>&1 | tail -15</automated>
    <automated>grep -nE "GPTQ|AWQ|SqueezeLLM|GGML Q" spectralstream/compression/certificate.py || echo "NO_LITERALS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "GPTQ|AWQ|SqueezeLLM|GGML Q" spectralstream/compression/certificate.py` returns nothing (NO_LITERALS).
    - `python -m tests/...` — `test_no_competitor_literals_in_certificate_source` now PASSES.
    - `python -m pytest tests/test_certificate.py tests/test_certificate_comprehensive.py -v` exits 0 (contract preserved).
    - `benchmark_industry_comparison.json` no longer exists at repo root (`ls benchmark_industry_comparison.json` fails).
    - `LITERATURE_DISCLAIMER` substring is present in `to_text()`/`to_markdown()` output (asserted by the contract test or a quick render check).
  </acceptance_criteria>
  <done>certificate.py is a pure consumer of literature_estimates; disclaimer surfaces; orphaned JSON deleted; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external competitor numbers → certificate rendering | literature_estimates is the only source; certificate.py consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing/Integrity | certificate.py industry comparison | high | mitigate | 9 competitor constants extracted to labeled literature_estimates; certificate.py imports them; no bare literals; disclaimer rendered. Test guards against literal leakage. |
| T-01-06 | Tampering/Integrity | orphaned benchmark_industry_comparison.json | medium | mitigate | File deleted (zero references); removes a fabricated, unmeasured artifact from the repo. Block on medium. |
</threat_model>

<verification>
- `python -m pytest tests/test_honest_metrics.py -v` exits 0.
- `python -m pytest tests/test_certificate.py tests/test_certificate_comprehensive.py -v` exits 0.
- Static: `grep -nE "GPTQ|AWQ|SqueezeLLM|GGML Q" spectralstream/compression/certificate.py` → no matches.
- `ls benchmark_industry_comparison.json` → file absent.
</verification>

<success_criteria>
- No fabricated competitor constants in certificate.py; external refs labeled "literature estimates, not measured here".
- industry_comparison contract preserved; orphaned JSON deleted.
- METRICS-03 tests pass.
</success_criteria>

<artifacts>
Artifacts this phase produces (new symbols created by Plan 03):
- `spectralstream/compression/literature_estimates.py` (NEW FILE) :: `LITERATURE_DISCLAIMER` (str), `LITERATURE_ESTIMATES` (List[Tuple[str,float,str,str]], 9 entries)
- `spectralstream/compression/certificate.py` :: import of `LITERATURE_ESTIMATES`/`LITERATURE_DISCLAIMER`; `_compute_industry_comparison` now builds comparisons from `LITERATURE_ESTIMATES` + runtime current-run row; adds `"disclaimer"` key to `self.industry_comparison`; renders disclaimer in `to_markdown` + `to_text`
- `tests/test_honest_metrics.py` :: `test_literature_disclaimer_present`, `test_no_competitor_literals_in_certificate_source`, `test_industry_comparison_contract_preserved` (appended)
- `benchmark_industry_comparison.json` :: DELETED (repo root)
</artifacts>

<output>
Create `.planning/phases/01-metrics-trust-loop/01-03-SUMMARY.md` when done
</output>
