---
phase: 01-metrics-trust-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spectralstream/compression/cli.py
  - tests/test_honest_metrics.py
autonomous: true
requirements:
  - METRICS-01
  - METRICS-02
user_setup: []
must_haves:
  truths:
    - BOTH CLI honest-metrics blocks route through apply_gate, so a high-error method is gated on every emission path (no leak).
    - The honest-ratio summary leads with ratio_vs_bf16 (labeled "(vs BF16 / disk)") and prints ratio_vs_fp32 only as a secondary line.
    - Gated tensors (ratio_vs_bf16 is None) are excluded from the BF16/FP32 means so they do not render as 0.0x, and a "GATED" marker line is emitted when any tensor is gated.
  artifacts:
    - cli.py second block (lines 1269-1295) gated via apply_gate
    - cli.py summary block (lines 1387-1425) BF16-led + None-filtered + GATED marker
    - tests/test_honest_metrics.py additions asserting bf16-first headline ordering + None handling
  key_links:
    - apply_gate is shared by both blocks; Pitfall 2 is that only one block gets gated.
    - Summary mean must tolerate None (gate output) or it breaks with a gated tensor.
---

<objective>
Finish the vertical slice from Plan 01: gate the SECOND CLI honest-metrics block (so no emission
path leaks a ratio past the gate), and make the CLI summary honest — lead with BF16 (vs disk),
demote FP32 to secondary, filter gated `None`s out of the mean, and render a "GATED" marker.

Purpose: METRICS-01 requires BOTH blocks gated; METRICS-02 requires BF16 as the headline baseline.
This is the user-visible end of the trust loop proven by Plan 01's gate.
Output: both CLI blocks gated; summary BF16-led with GATED handling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-metrics-trust-loop/01-RESEARCH.md
@.planning/phases/01-metrics-trust-loop/01-PATTERNS.md
@spectralstream/compression/cli.py
@spectralstream/compression/honest_metrics.py
@tests/test_honest_metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the second CLI block + reorder summary to BF16-led</name>
  <files>spectralstream/compression/cli.py, tests/test_honest_metrics.py</files>
  <read_first>
    - spectralstream/compression/cli.py (second honest-metrics block lines 1269-1295; summary block lines 1387-1425)
    - spectralstream/compression/honest_metrics.py (apply_gate, lines added by Plan 01)
    - tests/test_honest_metrics.py (existing tests from Plan 01)
  </read_first>
  <action>
    In `cli.py` the second block (lines 1269-1295): `apply_gate` is already imported from Plan 01.
    Replace the two ratio assignments (lines 1273-1275: the original baseline-ratio computation
      plus the two dict stores for fp32/bf16) with
      `err = end_to_end_error(tensor, recon)` (existing lines 1288-1292 compute err then assign the 4
      scalars) — keep those 4 scalar assignments, and add AFTER them:
      `honest_metrics_dict.update(apply_gate(data, tensor.size, err.rel_mse))`.
      Confirm there are now exactly 2 `apply_gate(` call sites (the per-baseline ratio helper
      must no longer appear in cli.py — both sites replaced). <!-- planner-discipline-allow: apply_gate( -->
    In the summary block (lines 1387-1425): the `hm_bf16`/`hm_fp32` collections (lines 1394-1398) currently
      do `.get("ratio_vs_bf16", 0)` — change the default to `None` (so a gated tensor yields `None`, not `0`).
      Reorder lines 1414-1417 so BF16 leads: filter Nones first
      (`hm_bf16_valid = [r for r in hm_bf16 if r is not None]` / `hm_fp32_valid = [r for r in hm_fp32 if r is not None]`),
      then `if hm_bf16_valid: logger.info("  Honest ratio (vs BF16 / disk): avg %.1fx", float(np.mean(hm_bf16_valid)))`,
      then `if hm_fp32_valid: logger.info("  (secondary, vs FP32): avg %.1fx", float(np.mean(hm_fp32_valid)))`.
      Add a "GATED" marker: if any `c.get("honest_metrics",{}).get("gated")` is truthy, emit
      `logger.info("  ⚠️ %d tensor(s) GATED — error exceeded threshold; ratio suppressed",
        sum(1 for c in compressed if c.get('honest_metrics',{}).get('gated')))`.
  </action>
  <verify>
    <automated>grep -c "dual_ratio(" spectralstream/compression/cli.py</automated>
    <automated>grep -c "apply_gate(" spectralstream/compression/cli.py</automated>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "gate or boundary or serialized or headline" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dual_ratio(" spectralstream/compression/cli.py` == 0 (both sites replaced by apply_gate).
    - `grep -c "apply_gate(" spectralstream/compression/cli.py` == 2 (World-Model block + second block).
    - `grep -n "vs BF16 / disk" spectralstream/compression/cli.py` returns the BF16-led summary line, printed BEFORE the "(secondary, vs FP32)" line.
    - `grep -n "GATED" spectralstream/compression/cli.py` returns the GATED marker log line.
    - pytest still exits 0 (existing Plan 01 tests unaffected).
  </acceptance_criteria>
  <done>Both CLI blocks gated; summary is BF16-led with None filtering and a GATED marker.</done>
</task>

<task type="auto">
  <name>Task 2: Add headline-ordering + None-filter test anchor</name>
  <files>tests/test_honest_metrics.py, spectralstream/compression/honest_metrics.py</files>
  <read_first>
    - tests/test_honest_metrics.py (Plan 01 content; section-banner style)
    - spectralstream/compression/honest_metrics.py (apply_gate return shape)
  </read_first>
  <action>
    Append to `tests/test_honest_metrics.py` (after existing tests) a section banner then cases
    anchoring METRICS-02 (BF16 headline) and the None-handling contract:
    - `test_bf16_is_headline_key`: for a good method, `apply_gate(...)` result dict has
      `"ratio_vs_bf16"` and `"ratio_vs_fp32"` both non-None; and `ratio_vs_bf16` key appears before
      `ratio_vs_fp32` is irrelevant — assert both present and that the headline is bf16 by checking
      `res["ratio_vs_bf16"] is not None` while gated is False (BF16 is the surfaced default).
    - `test_gated_ratio_is_none_not_zero`: for a bad method, `apply_gate(...)` returns
      `ratio_vs_bf16 is None` and `ratio_vs_fp32 is None` (so downstream mean filtering treats it as
      None, never 0.0x). Assert exactly that — the summary None-filter depends on None, not 0.
    - `test_gate_reason_format`: for `rel_mse=0.11, threshold=0.05`, `gate_reason == "rel_mse 0.1100 > 0.05"`.
    Mirror the module's box-drawing banner style and `from __future__ import annotations` (already present).
  </action>
  <verify>
    <automated>python -m pytest tests/test_honest_metrics.py -v -k "headline or none or gate_reason" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_honest_metrics.py` contains `def test_bf16_is_headline_key`, `def test_gated_ratio_is_none_not_zero`, `def test_gate_reason_format`.
    - The pytest command exits 0 (new tests pass).
    - `python -m pytest tests/test_honest_metrics.py -v` full file exits 0.
  </acceptance_criteria>
  <done>Test anchors for BF16 headline + gated-None (not zero) added and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compression output → CLI summary | Both blocks + summary rendering now flow through apply_gate results |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering/Integrity | cli.py second block + summary | medium | mitigate | Both blocks gated (no leak); summary filters None so gated tensors never render as 0.0x; GATED marker emitted. Block on medium. |
| T-01-04 | Spoofing/Integrity | cli.py summary ordering | low | accept | BF16-led ordering is a presentation-honesty fix; backed by apply_gate (already mitigated T-01-01). Accept because the underlying number is byte-exact and gated. |
</threat_model>

<verification>
- `python -m pytest tests/test_honest_metrics.py -v` exits 0.
- Static: `grep -c "dual_ratio(" spectralstream/compression/cli.py` == 0; `grep -c "apply_gate(" spectralstream/compression/cli.py` == 2.
</verification>

<success_criteria>
- Every CLI ratio emission path is gated; summary leads with BF16 and demotes FP32.
- Gated tensors are excluded from means and marked "GATED".
- Tests anchor BF16 headline + gated-None behavior.
</success_criteria>

<artifacts>
Artifacts this phase produces (new symbols created by Plan 02):
- `tests/test_honest_metrics.py` :: `test_bf16_is_headline_key`, `test_gated_ratio_is_none_not_zero`, `test_gate_reason_format` (appended)
- `cli.py` :: second honest-metrics block (L1269-1295) now calls `apply_gate`; summary block (L1387-1425) BF16-led with `hm_bf16_valid`/`hm_fp32_valid` None filters + GATED marker log line
</artifacts>

<output>
Create `.planning/phases/01-metrics-trust-loop/01-02-SUMMARY.md` when done
</output>
