---
phase: 02-eval-subsystem
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - benchmark_physics_real_weights.py
  - wave4_pipeline.py
  - README.md
autonomous: true
requirements:
  - EVAL-03
user_setup:
  - service: local_model
    why: "Scripts now read model path from SPECTRALSTREAM_MODEL_PATH or --model instead of a hardcoded absolute path."
    env_vars:
      - name: SPECTRALSTREAM_MODEL_PATH
        source: "Path to local .safetensors or .ssf model file"
    dashboard_config: []

must_haves:
  truths:
    - "No hardcoded `/home/mike/...` path remains in `benchmark_physics_real_weights.py` or `wave4_pipeline.py` (D-10)."
    - "`SPECTRALSTREAM_MODEL_PATH` env var and `--model` CLI arg are accepted by both scripts (D-10/D-11)."
    - "README documents the required Gemma-4 E2B model and how to supply it via env var or CLI."
    - "Resolution order is env var -> CLI arg -> relative fallback (`models/gemma-4-E2B/model.safetensors`)."
  artifacts:
    - benchmark_physics_real_weights.py
    - wave4_pipeline.py
    - README.md
  key_links:
    - "benchmark_physics_real_weights.py -> eval/model_path.resolve_model_path"
    - "wave4_pipeline.py -> eval/model_path.resolve_model_path"
    - "README.md -> SPECTRALSTREAM_MODEL_PATH / --model documentation"
---

## Phase Goal

**As a** researcher cloning the repository, **I want to** supply my own local model path via an environment variable or CLI flag, **so that** the hardcoded author-specific path is gone and I can reproduce results.

<objective>
Replace remaining hardcoded model paths with the shared `resolve_model_path` helper, add CLI `--model` support to both scripts, and update README to document the required Gemma-4 E2B model and how to supply it.

Purpose: Removes the environment-specific path leakage (SEC-02) and makes the phase reproducible on a fresh clone.
Output: Parameterized `benchmark_physics_real_weights.py` and `wave4_pipeline.py`, plus documented README instructions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-eval-subsystem/02-CONTEXT.md
@.planning/phases/02-eval-subsystem/02-RESEARCH.md
@.planning/phases/02-eval-subsystem/02-PATTERNS.md
@eval/model_path.py
@benchmark_physics_real_weights.py
@wave4_pipeline.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize benchmark_physics_real_weights.py</name>
  <files>benchmark_physics_real_weights.py, eval/model_path.py</files>

  <read_first>
    - `benchmark_physics_real_weights.py` lines 33-39 — current hardcoded `MemoryMappedTensorEngine` path.
    - `eval/model_path.py` from Plan 02-01 — shared `resolve_model_path` helper with env/CLI/fallback resolution and traversal validation.
    - `.planning/phases/02-eval-subsystem/02-CONTEXT.md` D-10, D-11 — path resolution requirements.
  </read_first>

  <action>
    Import `resolve_model_path` from `eval.model_path`. Add an `argparse` parser at the top of `main()` (or module entry) accepting `--model`. Resolve the model path with `resolve_model_path(args.model)` and pass the result to `MemoryMappedTensorEngine`. Remove the hardcoded `"/home/mike/Documents/Github/SpectralStream/models/gemma-4-E2B/model.safetensors"` string entirely. Keep all other script logic unchanged; the existing `signal.alarm`/`exec` timeout code is out of phase scope and must not be modified.
  </action>

  <acceptance_criteria>
    - `grep "/home/mike" benchmark_physics_real_weights.py` returns nothing.
    - `python benchmark_physics_real_weights.py --help` lists `--model`.
    - `python benchmark_physics_real_weights.py --model /nonexistent` raises `FileNotFoundError` before any model loading attempt.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "/home/mike" benchmark_physics_real_weights.py || true; test $(grep -c "/home/mike" benchmark_physics_real_weights.py || echo 0) -eq 0</automated>
  </verify>

  <done>
    - `benchmark_physics_real_weights.py` uses `resolve_model_path` and has no hardcoded absolute author path.
    - `--model` CLI flag is supported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize wave4_pipeline.py</name>
  <files>wave4_pipeline.py, eval/model_path.py</files>

  <read_first>
    - `wave4_pipeline.py` lines 164-166 — current hardcoded `model_path = "models/gemma-4-E2B/model.safetensors"`.
    - `eval/model_path.py` from Plan 02-01 — shared `resolve_model_path` helper.
  </read_first>

  <action>
    Import `resolve_model_path` from `eval.model_path`. Add `--model` to the existing `argparse` parser (or create one if absent). Replace the hardcoded `model_path = "models/gemma-4-E2B/model.safetensors"` line with `model_path = resolve_model_path(args.model)`. Keep the relative fallback behavior inside `resolve_model_path` so the default still works when `models/gemma-4-E2B/model.safetensors` exists.
  </action>

  <acceptance_criteria>
    - `grep "/home/mike" wave4_pipeline.py` returns nothing.
    - `python wave4_pipeline.py --help` lists `--model`.
    - `python wave4_pipeline.py --model /nonexistent` raises `FileNotFoundError`.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "/home/mike" wave4_pipeline.py || true; test $(grep -c "/home/mike" wave4_pipeline.py || echo 0) -eq 0</automated>
  </verify>

  <done>
    - `wave4_pipeline.py` uses `resolve_model_path` and has no hardcoded absolute author path.
    - `--model` CLI flag is supported.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document model path in README</name>
  <files>README.md</files>

  <read_first>
    - `README.md` lines 1-60 — current model/compression status paragraph and "Current status" line.
    - `.planning/phases/02-eval-subsystem/02-CONTEXT.md` D-11 — README must document required model and how to supply it.
  </read_first>

  <action>
    Add a new "Reproducing results" section near the top of `README.md` after the status paragraph. State that the repo assumes a local Gemma-4 E2B weights file, explain the resolution order `SPECTRALSTREAM_MODEL_PATH` env var -> `--model PATH` -> relative `models/gemma-4-E2B/model.safetensors` fallback, and give concrete shell examples: `export SPECTRALSTREAM_MODEL_PATH=/path/to/model.safetensors` and `python -m spectralstream.eval.run_eval --model /path/to/model.safetensors --compressed /path/to/model.ssf`. Keep the existing "Current status" line intact.
  </action>

  <acceptance_criteria>
    - `README.md` contains the string `SPECTRALSTREAM_MODEL_PATH`.
    - `README.md` contains the string `--model`.
    - `README.md` contains the string `Gemma-4 E2B`.
    - `README.md` contains the string `models/gemma-4-E2B/model.safetensors`.
  </acceptance_criteria>

  <verify>
    <automated>grep -E "SPECTRALSTREAM_MODEL_PATH|--model|Gemma-4 E2B|models/gemma-4-E2B/model.safetensors" README.md</automated>
  </verify>

  <done>
    - README documents the required model and both ways to supply it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> filesystem | `--model` crosses from user input to local file reads in two scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | `benchmark_physics_real_weights.py` / `wave4_pipeline.py` `--model` | high | mitigate | Both scripts delegate path validation to `eval/model_path.py` `resolve_model_path`, which rejects `..` traversal and verifies existence |
| T-02-03-02 | Information Disclosure | `README.md` | low | accept | Documents only user-supplied local path configuration; no secrets |
</threat_model>

<verification>
- `grep -rn "/home/mike" benchmark_physics_real_weights.py wave4_pipeline.py` returns nothing.
- `python benchmark_physics_real_weights.py --help` and `python wave4_pipeline.py --help` both list `--model`.
- `README.md` contains `SPECTRALSTREAM_MODEL_PATH`, `--model`, `Gemma-4 E2B`, and the relative fallback path.
</verification>

<success_criteria>
- No hardcoded absolute author paths remain in the two target scripts.
- Both scripts accept `--model` and honor `SPECTRALSTREAM_MODEL_PATH`.
- README clearly documents how a fresh clone supplies the required model.
</success_criteria>

<output>
Create `.planning/phases/02-eval-subsystem/02-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols and files created or modified by this plan:

- `benchmark_physics_real_weights.py` — new `--model` CLI flag; uses `eval.model_path.resolve_model_path`
- `wave4_pipeline.py` — new `--model` CLI flag; uses `eval.model_path.resolve_model_path`
- `README.md` — new "Reproducing results" section documenting `SPECTRALSTREAM_MODEL_PATH`, `--model`, and the fallback path
